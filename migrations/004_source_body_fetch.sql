-- Переключатель загрузки полного текста для конкретного издания.
-- Нужен для источников со стабильной антибот-защитой: качать у них тело —
-- впустую жечь запросы, анонса из фида достаточно (§3.2).
ALTER TABLE sources ADD COLUMN IF NOT EXISTS body_fetch BOOLEAN NOT NULL DEFAULT TRUE;
