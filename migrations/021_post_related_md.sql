-- Блок «Ранее по теме» переживает правку поста.
--
-- Он собирался при публикации и нигде не сохранялся, а правка (дополнение
-- ссылками, добавление карточки) пересобирает текст с нуля — и блок из поста
-- молча исчезал. Заметить это можно было только сравнив пост до и после.

ALTER TABLE posts ADD COLUMN IF NOT EXISTS related_md TEXT NOT NULL DEFAULT '';
